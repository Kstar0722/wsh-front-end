import { SyndicateSeparatorPipe } from './syndicate-separator.pipe';

describe('SyndicateSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new SyndicateSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
