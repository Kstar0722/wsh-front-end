import { CountriesSeparatorPipe } from './countries-separator.pipe';

describe('CountriesSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new CountriesSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
