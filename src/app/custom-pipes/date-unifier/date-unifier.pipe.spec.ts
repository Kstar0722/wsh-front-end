import { DateUnifierPipe } from './date-unifier.pipe';

describe('DateUnifierPipe', () => {
  it('create an instance', () => {
    const pipe = new DateUnifierPipe();
    expect(pipe).toBeTruthy();
  });
});
